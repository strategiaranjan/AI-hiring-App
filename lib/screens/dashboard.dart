import 'package:flutter/material.dart';

class Dashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Strategia Advizo Dashboard')),
      body: Center(
        child: Text('Welcome to Strategia Advizo AI Hiring App!'),
      ),
    );
  }
}
